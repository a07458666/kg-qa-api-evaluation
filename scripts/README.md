# Scripts Placeholder

Execution scripts are intentionally not implemented yet.

Planned scripts:
- `run_eval.py`: validate config/cases, call backend APIs, emit a report JSON
- `compare_reports.py`: compare current and baseline reports, emit regression summary

These will be added after the real backend request/response contract is available.
